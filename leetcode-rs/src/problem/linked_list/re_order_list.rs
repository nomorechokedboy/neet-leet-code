use crate::problem::linked_list::list_node::ListNode;

pub fn reorder_list(head: &mut Option<Box<ListNode>>) {
    let mut track = head.as_ref();
    while head.is_some() {
        let webb = head..unwrap().next;
    }
    ()
}
